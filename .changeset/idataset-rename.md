---
"@_linked/server": minor
---

Update `BackendAPIStore` to implement `IDataset` (renamed from `IQuadStore` in `@_linked/core`).

No functional change — import path and interface name updated to match the new `@_linked/core` export.
